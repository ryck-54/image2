import { Template } from "../components/Template"

export default function formularioPage() {
  return(
    <main>
       <h1>Formulário</h1>
      <Template>
      <h1>informação do formulário</h1>
      </Template>
    </main>
  );
}